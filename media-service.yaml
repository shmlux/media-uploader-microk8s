apiVersion: v1
kind: Service
metadata:
  name: media-service
spec:
  type: NodePort
  selector:
    app: media-uploader
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080

